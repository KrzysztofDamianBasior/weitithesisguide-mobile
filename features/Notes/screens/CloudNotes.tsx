import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const CloudNotes = (props: Props) => {
  return (
    <View>
      <Text>CloudNotes</Text>
    </View>
  );
};

export default CloudNotes;

const styles = StyleSheet.create({});
